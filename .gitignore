# Diagnosis notes and the NVIDIA report - kept local, not published here.
FINDINGS.md
report/

# Runtime logs from run.sh / *.sh - regenerated on every run.
logs/

# Kernel module build output - regenerated by module/Makefile into build/.
build/

# Compiled/kbuild artifacts that used to live next to the module source.
# Only egpu_rp_window.c and Makefile are sources; everything else here is
# generated. See module/Makefile for why the build now happens in build/.
module/*.o
module/*.ko
module/*.mod
module/*.mod.c
module/*.mod.o
module/*.symvers
module/modules.order
module/.*.cmd
module/..*.cmd

# Editor/OS noise
.DS_Store
*.swp
